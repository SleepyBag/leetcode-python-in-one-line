class Solution:
    def hasCycle(self, head: ListNode) -> bool:
        return False if head is None else True if (head.val == 'foobar', head.__setattr__('val', 'foobar'))[0] else self.hasCycle(head.next)
